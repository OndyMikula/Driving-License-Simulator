using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CanvasManager : MonoBehaviour
{
    public static GameObject FailCanvas { get; set; }
    public static GameObject SuccessCanvas { get; set; }
    public static GameObject CheckpointCanvas { get; set; }
    public static GameObject SpeedMonitorCanvas { get; set; }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
